.SearchReview
    .SearchField
        = form_with(url: search_reviews_path, local: true, method: :get, class: 'search-form') do |f|
            = f.text_field :keyword, placeholder: 'find a review', class: 'search-input'
            = f.submit "search", class: 'search-btn'
    .Contents
        - @reviews.each do |review|
            = link_to review.title, review_path(review.id), class: 'result-box'